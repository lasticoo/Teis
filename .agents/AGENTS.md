# Workspace Guidelines & Rules

- **Git Push Policy**: DO NOT execute `git push` (or push changes to GitHub) unless explicitly instructed by the user. Local commits or building containers locally are allowed, but pushing to remote GitHub requires explicit user permission.
